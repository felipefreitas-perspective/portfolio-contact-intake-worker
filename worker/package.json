{
  "name": "portfolio-contact-intake-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute portfolio_contact_intake --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute portfolio_contact_intake --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
